<div class="container row row-cols-1 row-cols-md-3">
    <div class="row" th:each="image : ${company.imageList}">
        <div class="col-md-3 col-sm-4 col-xs-6">
            <img th:src="${image.url}" th:width="250" th:height="150" th:hspace="15" th:vspace="15" alt=""/>
        </div>
    </div>
</div>





    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active" th:each="image : ${company.imageList}">
                <img th:src="${image.url}" th:width="200" th:height="200">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>